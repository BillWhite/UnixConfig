#!/bin/bash

touch $HOME/.startup.log
echo xsessionrc on $(date) >> "$HOME/.startup.log"

. "$HOME/.bash_functions"
echo start xsessionrc on $(date) &>> "$HOME/.startup.log"
read_bash_init_files "$HOME/.xsessionrc.d" -x &>> $HOME/.startup.log
echo end xsessionrc on $(date) &>> "$HOME/.startup.log"
